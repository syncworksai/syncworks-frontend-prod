import React, { useEffect, useMemo, useState } from "react";
import { archiveExpiredCloudWorkout, loadCloudActiveWorkout } from "./healthWorkoutCloudSync";
import { currentDayWorkout, formatHealthDay, localYmd, shouldOfferPreviousWorkout } from "./healthWorkoutDateLifecycle";
import WorkoutConsistencyCard from "./WorkoutConsistencyCard";
import HealthGoalBriefing from "./HealthGoalBriefing";

const WORKOUT_TEMPLATES = {
  strength: { title: "Chest Push Focus", subtitle: "Chest, shoulders, triceps", minutes: 48, exercises: [["Bench Press", "4 x 6-8", "https://www.youtube.com/results?search_query=bench+press+form"], ["Incline Dumbbell Press", "3 x 8-10", "https://www.youtube.com/results?search_query=incline+dumbbell+press+form"], ["Seated Shoulder Press", "3 x 8-10", "https://www.youtube.com/results?search_query=seated+shoulder+press+form"], ["Cable Chest Fly", "3 x 10-12", "https://www.youtube.com/results?search_query=cable+chest+fly+form"], ["Triceps Pushdown", "3 x 10-12", "https://www.youtube.com/results?search_query=triceps+pushdown+form"]] },
  pull: { title: "Back Pull Focus", subtitle: "Back, lats, rear delts, biceps", minutes: 50, exercises: [["Lat Pulldown", "4 x 8-10", "https://www.youtube.com/results?search_query=lat+pulldown+form"], ["Seated Row", "4 x 8-10", "https://www.youtube.com/results?search_query=seated+row+form"], ["Single Arm Dumbbell Row", "3 x 10", "https://www.youtube.com/results?search_query=single+arm+dumbbell+row+form"], ["Face Pull", "3 x 12-15", "https://www.youtube.com/results?search_query=face+pull+form"], ["Biceps Curl", "3 x 10-12", "https://www.youtube.com/results?search_query=biceps+curl+form"]] },
  cardio: { title: "Cardio + Abs Focus", subtitle: "Conditioning, core, endurance", minutes: 36, exercises: [["Incline Walk", "10 min", "https://www.youtube.com/results?search_query=incline+treadmill+walking"], ["Bike Intervals", "8 rounds", "https://www.youtube.com/results?search_query=stationary+bike+intervals"], ["Plank", "3 x 45 sec", "https://www.youtube.com/results?search_query=plank+proper+form"], ["Dead Bug", "3 x 10/side", "https://www.youtube.com/results?search_query=dead+bug+exercise+form"], ["Hanging Knee Raise", "3 x 10", "https://www.youtube.com/results?search_query=hanging+knee+raise+form"]] },
  mobility: { title: "Mobility + Recovery Focus", subtitle: "Hips, shoulders, spine, recovery", minutes: 28, exercises: [["90/90 Hip Switch", "2 x 8/side", "https://www.youtube.com/results?search_query=90+90+hip+switch"], ["World's Greatest Stretch", "2 x 5/side", "https://www.youtube.com/results?search_query=worlds+greatest+stretch"], ["Thoracic Rotation", "2 x 8/side", "https://www.youtube.com/results?search_query=thoracic+rotation+mobility"], ["Band Shoulder Dislocates", "2 x 12", "https://www.youtube.com/results?search_query=band+shoulder+mobility"], ["Couch Stretch", "2 x 45 sec", "https://www.youtube.com/results?search_query=couch+stretch"]] },
  hiit: { title: "HIIT / Tabata Focus", subtitle: "Intervals adaptable to any level", minutes: 24, exercises: [["Work Interval", "20 sec", "https://www.youtube.com/results?search_query=beginner+tabata+workout"], ["Rest Interval", "10 sec", "https://www.youtube.com/results?search_query=tabata+timer+workout"], ["Repeat", "8 rounds", "https://www.youtube.com/results?search_query=tabata+8+rounds"], ["Recovery Walk", "3 min", "https://www.youtube.com/results?search_query=active+recovery+walk"]] },
};
function safeNumber(value, fallback = 0) { const parsed = Number(value); return Number.isFinite(parsed) ? parsed : fallback; }
function pct(value, goal) { return goal ? Math.max(0, Math.min(100, Math.round((value / goal) * 100))) : 0; }
function MetricBar({ label, value, goal, suffix = "", purple = false, onSetGoal }) { const width = pct(value, goal); return <div><div className="flex items-center justify-between gap-3 text-xs"><span className={purple ? "font-bold text-violet-200" : "font-bold text-cyan-200"}>{label}</span><span className="font-black text-white">{Math.round(value)}{suffix}<span className="font-semibold text-slate-500"> {goal ? `/ ${Math.round(goal)}${suffix}` : "/ target not set"}</span></span></div><div className="mt-2 h-2 overflow-hidden rounded-full bg-white/[0.07]"><div className={purple ? "h-full rounded-full bg-gradient-to-r from-violet-500 to-fuchsia-500" : "h-full rounded-full bg-gradient-to-r from-blue-500 to-cyan-400"} style={{ width: `${width}%` }} /></div>{!goal && onSetGoal ? <button type="button" onClick={onSetGoal} className="mt-2 text-[10px] font-black text-cyan-300">Set target →</button> : null}</div>; }
function LogoButton({ onClick, small = false }) { return <button type="button" onClick={onClick} className={`${small ? "h-16 w-16" : "h-28 w-28 sm:h-32 sm:w-32"} relative flex shrink-0 items-center justify-center rounded-full border border-blue-300/70 bg-[#050914] shadow-[0_0_18px_rgba(37,99,235,.8),0_0_42px_rgba(124,58,237,.55)]`}><span className="absolute inset-2 rounded-full border border-violet-400/70" /><span className={`${small ? "text-3xl" : "text-5xl"} bg-gradient-to-br from-cyan-300 via-blue-500 to-fuchsia-500 bg-clip-text font-black italic text-transparent`}>S</span></button>; }
function TimerCard() { const [seconds, setSeconds] = useState(60); const [running, setRunning] = useState(false); const [mode, setMode] = useState("Rest"); useEffect(() => { if (!running || seconds <= 0) return undefined; const id = window.setInterval(() => setSeconds((v) => Math.max(0, v - 1)), 1000); return () => window.clearInterval(id); }, [running, seconds]); useEffect(() => { if (seconds === 0) setRunning(false); }, [seconds]); function choose(next) { setMode(next); setRunning(false); setSeconds(next === "Tabata" ? 20 : next === "HIIT" ? 40 : 60); } return <section className="rounded-3xl border border-blue-400/20 bg-[#070d1b] p-4"><div className="flex items-center justify-between"><div><div className="text-[10px] font-black uppercase tracking-[.2em] text-blue-300">Workout Timer</div><div className="mt-1 text-lg font-black text-white">{mode}</div></div><div className="text-3xl font-black tabular-nums text-cyan-300">{String(Math.floor(seconds / 60)).padStart(2, "0")}:{String(seconds % 60).padStart(2, "0")}</div></div><div className="mt-4 grid grid-cols-3 gap-2">{["Rest", "Tabata", "HIIT"].map((item) => <button key={item} type="button" onClick={() => choose(item)} className={`h-9 rounded-xl border text-xs font-black ${mode === item ? "border-blue-400/60 bg-blue-500/20 text-blue-100" : "border-white/10 bg-white/[.03] text-slate-400"}`}>{item}</button>)}</div><div className="mt-3 grid grid-cols-2 gap-2"><button type="button" onClick={() => setRunning((v) => !v)} className="h-10 rounded-xl bg-gradient-to-r from-blue-500 to-cyan-400 text-sm font-black text-white">{running ? "Pause" : "Start"}</button><button type="button" onClick={() => { setRunning(false); choose(mode); }} className="h-10 rounded-xl border border-white/10 bg-white/[.04] text-sm font-black text-white">Reset</button></div></section>; }
function WorkoutMenu({ onOpen, onStartWorkout }) { const [kind, setKind] = useState("strength"); const [completed, setCompleted] = useState([]); const workout = WORKOUT_TEMPLATES[kind]; const completion = Math.round((completed.length / workout.exercises.length) * 100); useEffect(() => setCompleted([]), [kind]); return <div className="grid gap-4 xl:grid-cols-[1fr_330px]"><div className="rounded-[2rem] border border-blue-400/20 bg-[#050b18] p-4 sm:p-5"><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><div className="text-[10px] font-black uppercase tracking-[.22em] text-blue-300">Quick Start Templates</div><h2 className="mt-1 text-2xl font-black text-white">{workout.title}</h2><p className="mt-1 text-sm text-slate-400">{workout.subtitle}</p></div><button type="button" onClick={() => onStartWorkout?.({ workout_name: workout.title, duration_minutes: workout.minutes, exercises: workout.exercises.map(([name, target]) => ({ name, target })) })} className="h-11 rounded-2xl bg-gradient-to-r from-cyan-400 via-blue-500 to-violet-600 px-5 text-sm font-black text-white">Start Template</button></div><div className="mt-4 grid grid-cols-2 gap-2 sm:grid-cols-5">{[["strength", "Push"], ["pull", "Pull"], ["cardio", "Cardio + Abs"], ["mobility", "Mobility"], ["hiit", "HIIT / Tabata"]].map(([id, label]) => <button key={id} type="button" onClick={() => setKind(id)} className={`rounded-2xl border px-3 py-2 text-xs font-black ${kind === id ? "border-blue-400/60 bg-blue-500/20 text-blue-100" : "border-white/10 bg-white/[.03] text-slate-400"}`}>{label}</button>)}</div><div className="mt-5 flex items-center justify-between text-xs font-bold text-slate-400"><span>{completed.length} / {workout.exercises.length} previewed</span><span>{completion}%</span></div><div className="mt-2 h-2 overflow-hidden rounded-full bg-white/[.06]"><div className="h-full rounded-full bg-gradient-to-r from-blue-500 to-cyan-400" style={{ width: `${completion}%` }} /></div><div className="mt-4 space-y-3">{workout.exercises.map(([name, target, video], index) => { const done = completed.includes(index); return <article key={name} className="rounded-2xl border border-white/10 bg-[#09111f] p-3"><div className="flex items-center gap-3"><button type="button" onClick={() => setCompleted((items) => done ? items.filter((item) => item !== index) : [...items, index])} className={`flex h-8 w-8 shrink-0 items-center justify-center rounded-full border text-xs font-black ${done ? "border-blue-300 bg-blue-500 text-white" : "border-white/20 text-slate-500"}`}>{done ? "✓" : index + 1}</button><div className="min-w-0 flex-1"><div className="font-black text-white">{name}</div><div className="mt-1 text-xs text-slate-500">{target}</div></div></div><details className="mt-3 rounded-xl border border-white/10 bg-black/20 px-3 py-2"><summary className="cursor-pointer text-xs font-bold text-slate-300">Video guidance</summary><a href={video} target="_blank" rel="noreferrer" className="mt-2 inline-flex text-xs font-black text-cyan-300">Open YouTube form guide →</a></details></article>; })}</div></div><aside className="space-y-4"><section className="rounded-3xl border border-violet-400/20 bg-gradient-to-br from-violet-500/10 to-blue-500/10 p-4"><div className="flex items-center gap-3"><LogoButton small onClick={() => onOpen?.("coach-chat")} /><div><div className="text-[10px] font-black uppercase tracking-[.2em] text-violet-200">SYNC Coach</div><div className="mt-1 font-black text-white">Adapt today's workout</div></div></div><p className="mt-3 text-sm leading-6 text-slate-300">Tell SYNC what changed and adapt the session.</p><button type="button" onClick={() => onOpen?.("coach-chat")} className="mt-3 h-10 w-full rounded-xl bg-gradient-to-r from-blue-500 to-violet-600 text-xs font-black text-white">Talk to SYNC</button></section><TimerCard /><button type="button" onClick={() => onOpen?.("nutrition-dashboard")} className="w-full rounded-3xl border border-blue-400/20 bg-[#070d1b] p-4 text-left"><div className="text-xs font-black uppercase tracking-[.18em] text-blue-300">Nutrition</div><div className="mt-2 text-lg font-black text-white">Fuel this workout</div></button></aside></div>; }

export default function HealthDashboard({ profile = {}, snapshot = {}, history = [], onOpen, onStartWorkout }) {
  const [screen, setScreen] = useState("dashboard"); const [dayKey, setDayKey] = useState(() => localYmd()); const [previousWorkout, setPreviousWorkout] = useState(null);
  const name = String(profile?.first_name || profile?.name || "").trim().split(/\s+/)[0] || "there"; const calories = safeNumber(snapshot?.calories ?? snapshot?.calories_today, 0); const calorieGoal = safeNumber(snapshot?.calorie_goal ?? profile?.calorie_goal, 0); const protein = safeNumber(snapshot?.protein_today ?? snapshot?.protein, 0); const proteinGoal = safeNumber(snapshot?.protein_goal ?? profile?.protein_goal, 0); const weekPlan = Array.isArray(snapshot?.week_plan) ? snapshot.week_plan : []; const todayWorkout = currentDayWorkout(weekPlan, dayKey); const upcomingWorkout = [...weekPlan].filter((item) => item?.workout_name && !["Completed", "Skipped", "Rescheduled"].includes(item?.status)).filter((item) => String(item?.ymd || "") >= dayKey).sort((a,b) => String(a?.ymd || "9999").localeCompare(String(b?.ymd || "9999")))[0]; const next = todayWorkout || upcomingWorkout || null; const completedPlanItems = weekPlan.filter((item) => item?.status === "Completed").length; const scheduledPlanItems = weekPlan.filter((item) => item?.workout_name).length; const planPct = Math.max(0, Math.min(100, safeNumber(snapshot?.plan_completion_percent, scheduledPlanItems ? Math.round((completedPlanItems / scheduledPlanItems) * 100) : 0))); const todayTitle = next?.workout_name || "No workout planned"; const todayMinutes = safeNumber(next?.duration_minutes ?? next?.minutes, 0); const todayExercises = Array.isArray(next?.exercises) ? next.exercises.length : 0; const todayFocus = next?.focus || next?.note || next?.muscle_focus || (next ? "Planned session" : "Build or choose a session for today");
  const navItems = useMemo(() => [["dashboard", "Home"], ["workouts", "Workouts"], ["nutrition-dashboard", "Nutrition"], ["progress", "Progress"], ["shop", "Shop"]], []);
  useEffect(() => { let cancelled = false; async function refreshWorkoutDay() { const today = localYmd(); setDayKey(today); try { const active = await loadCloudActiveWorkout(); if (cancelled || !active?.session) { if (!cancelled) setPreviousWorkout(null); return; } const lifecycle = shouldOfferPreviousWorkout(active); if (lifecycle.expired) { await archiveExpiredCloudWorkout(active); if (!cancelled) setPreviousWorkout(null); return; } if (lifecycle.ageDays === 1) { if (!cancelled) setPreviousWorkout({ ...active, session: lifecycle.session, label: lifecycle.label }); return; } if (!cancelled) setPreviousWorkout(null); } catch (error) { console.warn("Health date rollover check unavailable.", error); } } refreshWorkoutDay(); const interval = window.setInterval(refreshWorkoutDay, 60000); const handleVisibility = () => { if (document.visibilityState === "visible") refreshWorkoutDay(); }; document.addEventListener("visibilitychange", handleVisibility); return () => { cancelled = true; window.clearInterval(interval); document.removeEventListener("visibilitychange", handleVisibility); }; }, []);
  const resumePreviousWorkout = previousWorkout ? { ...previousWorkout.session, id: previousWorkout.planner_item_id || previousWorkout.session?.planner_item_id || previousWorkout.session?.id, workout_id: previousWorkout.workout_id || previousWorkout.session?.workout_id || "", ymd: previousWorkout.session?.scheduled_ymd || previousWorkout.session?.ymd || "", workout_name: previousWorkout.session?.workout_name || "Previous workout" } : null;
  return <div className="min-h-screen rounded-[2rem] border border-blue-400/15 bg-[radial-gradient(circle_at_top_left,rgba(37,99,235,.13),transparent_30%),linear-gradient(180deg,#030711,#050a14)] p-3 text-white sm:p-5"><div className="grid gap-5 lg:grid-cols-[190px_1fr]"><aside className="hidden rounded-[1.75rem] border border-white/10 bg-[#060b16] p-4 lg:block"><div className="text-xs font-black tracking-[.22em] text-white">SYNCWORKS</div><div className="mt-1 text-[9px] font-black tracking-[.3em] text-violet-400">HEALTH</div><nav className="mt-8 space-y-2">{navItems.map(([id,label]) => <button key={id} type="button" onClick={() => id === "dashboard" || id === "workouts" ? setScreen(id) : onOpen?.(id)} className={`h-11 w-full rounded-xl px-3 text-left text-sm font-black ${screen === id ? "bg-blue-500/15 text-blue-200" : "text-slate-400"}`}>{label}</button>)}</nav></aside><main className="min-w-0 pb-6"><header className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><div className="text-[10px] font-black uppercase tracking-[.18em] text-cyan-300">{formatHealthDay(dayKey)}</div><h1 className="mt-1 text-3xl font-black tracking-tight">Good day, <span className="text-blue-400">{name}</span></h1><p className="mt-1 text-sm text-slate-400">Health should explain the plan, then turn it into actions you can actually execute.</p></div><button type="button" onClick={() => onOpen?.("planner")} className="h-10 rounded-xl border border-blue-400/20 bg-blue-500/10 px-3 text-xs font-black text-blue-200">Plan</button></header>{screen === "workouts" ? <div className="mt-5"><WorkoutMenu onOpen={onOpen} onStartWorkout={onStartWorkout} /></div> : <>{previousWorkout && resumePreviousWorkout ? <section className="mt-5 rounded-[1.5rem] border border-amber-300/25 bg-amber-300/[0.07] p-4"><div className="text-[9px] font-black uppercase text-amber-200">{previousWorkout.label || "Yesterday - Incomplete"}</div><div className="mt-1 text-lg font-black">{resumePreviousWorkout.workout_name}</div><button type="button" onClick={() => onStartWorkout?.(resumePreviousWorkout)} className="mt-3 h-10 rounded-xl border border-amber-300/35 bg-amber-300/15 px-4 text-xs font-black text-amber-100">Resume Yesterday</button></section> : null}<div className="mt-5 grid gap-4 xl:grid-cols-3"><section className="rounded-[1.75rem] border border-blue-400/20 bg-[#07101e] p-5"><div className="text-[10px] font-black uppercase text-blue-300">{todayWorkout ? "Today's Workout" : next ? "Next Workout" : "Workout Plan"}</div><h2 className="mt-2 text-2xl font-black">{todayTitle}</h2><p className="mt-1 text-sm text-slate-400">{todayFocus}</p><div className="mt-5 grid grid-cols-3 gap-2 text-center"><div><b>{todayMinutes || "-"}</b><div className="text-[10px] text-slate-500">Minutes</div></div><div><b>{todayExercises || "-"}</b><div className="text-[10px] text-slate-500">Exercises</div></div><div><b>{next?.level || "Adaptive"}</b><div className="text-[10px] text-slate-500">Level</div></div></div><button type="button" onClick={() => next ? onStartWorkout?.(next) : onOpen?.("plan-today")} className="mt-5 h-11 w-full rounded-xl bg-gradient-to-r from-cyan-400 via-blue-500 to-violet-600 text-sm font-black">{next ? "Start Planned Workout" : "Plan Today's Workout"}</button></section><section className="rounded-[1.75rem] border border-white/10 bg-[#07101e] p-5"><div className="text-[10px] font-black uppercase text-slate-400">Nutrition Summary</div><div className="mt-5 space-y-5"><MetricBar label="Calories" value={calories} goal={calorieGoal} onSetGoal={() => onOpen?.("nutrition-goals")} /><MetricBar label="Protein" value={protein} goal={proteinGoal} suffix="g" purple onSetGoal={() => onOpen?.("nutrition-goals")} /></div><button type="button" onClick={() => onOpen?.("nutrition-dashboard")} className="mt-6 text-xs font-black text-cyan-300">Open nutrition center →</button></section><section className="rounded-[1.75rem] border border-white/10 bg-[#07101e] p-5"><div className="text-[10px] font-black uppercase text-slate-400">Plan Progress</div><div className="mt-4 text-4xl font-black">{planPct}%</div><div className="mt-2 text-sm text-slate-400">{scheduledPlanItems ? `${completedPlanItems} of ${scheduledPlanItems} planned sessions` : "No weekly plan yet"}</div><button type="button" onClick={() => onOpen?.("planner")} className="mt-3 text-xs font-black text-blue-300">View plan →</button></section></div><div className="mt-4"><HealthGoalBriefing profile={profile} snapshot={snapshot} onOpenNutrition={() => onOpen?.("nutrition-dashboard")} onOpenPlanner={() => onOpen?.("planner")} /></div><div className="mt-4"><WorkoutConsistencyCard weekPlan={weekPlan} dayKey={dayKey} onOpenPlan={() => onOpen?.("planner")} /></div><div className="mt-4 grid gap-4 xl:grid-cols-2"><section className="rounded-[1.75rem] border border-blue-400/20 bg-gradient-to-br from-blue-500/10 to-violet-500/10 p-5"><div className="flex items-center gap-3"><LogoButton small onClick={() => onOpen?.("coach-chat")} /><div><div className="text-[10px] font-black uppercase text-blue-300">SYNC AI Coach</div><h3 className="mt-1 text-xl font-black">Coach, planner and accountability partner.</h3></div></div><button type="button" onClick={() => onOpen?.("coach-chat")} className="mt-4 h-10 rounded-xl bg-gradient-to-r from-blue-500 to-violet-600 px-4 text-xs font-black">Chat with SYNC</button></section><TimerCard /></div></>}</main></div></div>;
}
