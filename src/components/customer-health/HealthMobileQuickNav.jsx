// src/components/customer-health/HealthMobileQuickNav.jsx
import React from "react";

/**
 * Health previously rendered its own fixed five-button mobile navigation.
 * That duplicated the global Personal navigation and could cover Health
 * content on smaller phones. Health shortcuts now live directly below the
 * standard SyncWorks header, while RoleAwareMobileNav remains the single
 * fixed navigation bar across the Personal experience.
 */
export default function HealthMobileQuickNav() {
  return null;
}
